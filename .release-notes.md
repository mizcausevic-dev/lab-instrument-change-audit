# v0.1-shipped

- initial public ship of `lab-instrument-change-audit`
- C# / ASP.NET biotech operator surface for instrument changes, firmware/calibration gaps, and audit-safe attestation posture
- live routes: `/`, `/instrument-lane`, `/change-log`, `/control-posture`, `/verification`, `/docs`
- synthetic sample data only
- Pages deploy with `instruments.kineticgain.com`
